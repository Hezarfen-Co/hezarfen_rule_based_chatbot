# Çekirdek asistan (engine/rules/similarity/safety) yalnızca stdlib kullanır;
# aşağıdaki tek bağımlılık SADECE backend'e bağlanan QUIC köprüsü (src/bridge.py)
# içindir. Köprüyü çalıştırmıyorsanız (ör. --validate/--evaluate/--chat) gerek yoktur.
aioquic==1.3.0
